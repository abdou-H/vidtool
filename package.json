{
  "name": "vidtool",
  "version": "1.0.0",
  "description": "Video Downloader Tool using Node.js and FFmpeg",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "apt-get update && apt-get install -y ffmpeg python3-pip && pip3 install yt-dlp"
  },
  "dependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "cors": "^2.8.5",
    "ejs": "^3.1.9",
    "express": "^4.18.2"
  }
}
